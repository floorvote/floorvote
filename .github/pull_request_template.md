## What changed and why

<!-- Describe the change and the reason for it. -->

## Checklist

- [ ] Commits are signed off (`git commit -s`) — see the [DCO](../blob/main/DCO)
- [ ] `npm test` passes in `api/`, `central/`, and `web/`
- [ ] `npm run build` passes from `web/`
- [ ] Change is focused on one feature or fix
